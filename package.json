{
  "name": "mysite",
  "private": true,
  "scripts": {
    "lint": "biome lint .",
    "format": "prettier -w './**/*.{md,yml}' && biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "prettier": "^3.1.1",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
